Open the uploaded workbook `1_585-41_init.xlsx`.

In `Sheet1`, clean the values in range `A1:H1` so that non-numeric characters are removed while preserving numeric digits and decimal points. Existing numeric cells should remain numeric. Text cells such as currency-formatted or symbol-contaminated values should become numeric values, not text strings.

Save the completed workbook as `/workspace/answer.xlsx`.
